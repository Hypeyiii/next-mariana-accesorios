import { Roboto, Inter, Karla, Teko, Lusitana } from "next/font/google";

const roboto = Roboto({ weight: "400", subsets: ["latin"] });

const inter = Inter({ weight: "400", subsets: ["latin"] });

const karla = Karla({ weight: "400", subsets: ["latin"] });

const teko = Teko({ weight: "400", subsets: ["latin"] });

const lusitana = Lusitana({ weight: "400", subsets: ["latin"] });

export { inter, roboto, karla, teko, lusitana };
